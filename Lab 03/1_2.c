/// area of circle = pi * r * r
#include <stdio.h>

int main()
{
    float radius = 7.0;
    float pi = 3.1416;
    float area = pi * radius * radius;

    printf("Radius : %.1f\n", radius);
    printf("Area   : %.2f\n", area)

    return 0;
}
